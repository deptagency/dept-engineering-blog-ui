import styled from '@emotion/styled';
import { spaces } from '@components/common/spaces';
import { Headline } from '../text/Headlines'

export const PostCardHeadline = styled(Headline)`
  margin: 0 0 ${spaces.sm}px;
`;
