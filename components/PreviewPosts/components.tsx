import styled from '@emotion/styled';
import { Headline } from '@components/text/Headlines';
import { spaces } from '@components/common/spaces';

export const PreviewPostsHeadline = styled(Headline)`
  margin: 0 0 ${spaces.sm}px;
`;
